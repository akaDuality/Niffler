import SwiftUI

struct ProfileView: View {
    var body: some View {
        Text("This is the Profile View")
            .navigationBarTitle("Profile", displayMode: .inline)
    }
}

#Preview {
    ProfileView()
}
